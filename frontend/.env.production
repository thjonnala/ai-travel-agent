# Consolidated Thiru apps API (Azure App Service). The travel agent's routes live
# under /api/sat (applied in the request paths), so this is the origin only.
VITE_API_BASE_URL=https://thiru-apps-api.azurewebsites.net
