# .NET
bin/
obj/
*.user
.vs/

# Node / Vite
node_modules/
dist/
*.local

# Environment / secrets — never commit real values
.env
.env.*
!.env.example
appsettings.*.Local.json

# OS noise
Thumbs.db
.DS_Store

# Python
__pycache__/

# Frontend prod API URL is public config, not a secret — must be committed for CI builds.
!frontend/.env.production
